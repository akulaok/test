export enum AppRoute {
  Students = '/',
}

export enum ApiRoute {
  Persons = 'persons',
}

export const FilterTypesArr = ['Имя А-Я', 'Имя Я-А', 'Сначала моложе', 'Сначала старше', 'Высокий рейтинг', 'Низкий рейтинг'];
